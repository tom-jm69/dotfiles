#!/bin/bash

alias vim="nvim"
alias n="nvim"
alias q="exit"
alias lg="lazygit"
alias hosts='cat /etc/hosts | tr " " "\n" | fzf'
alias ls='eza --group-directories-first --icons'
alias r='fc -s'
alias pacman="sudo pacman"
alias hashpw="~/dotfiles/scripts/hash.sh"

# scripts
#alias tmux='~/scripts/tmux-wrapper.sh'
alias sshs='lazyssh'

alias cat="bat -p"
alias act='source env/bin/activate'

sudo() {
    command sudo -E -s "$@"
}

# Print all 256 colors
colors() {
    local i
    for i in {0..255}; do
        printf "\x1b[38;5;${i}mcolor %d\n" "$i"
    done
    tput sgr0
}
